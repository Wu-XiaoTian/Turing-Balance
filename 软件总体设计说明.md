# Turing Balance（图灵天平）软件总体设计说明

## 1. 项目概述

Turing Balance 是一个用于测试不同 AI 的智商和情商，并支持 AI 伴侣匹配推荐的软件系统。系统前端部署在 Vercel，后端使用 Supabase 提供认证、数据库与服务能力。

系统以“AI 智商/情商评估”和“华清池 AI 伴侣匹配”两条业务主线为核心，围绕用户登录注册、问卷采集、AI 回答、评分汇总、匹配排序和报告生成构建完整流程。

## 2. 设计依据

本设计基于以下 UML 文档整理：

- [类图UML.txt](软件详细设计/数据设计和子系统设计/子系统设计/类图/类图UML.txt)
- [AI智商情商状态图UML.txt](软件详细设计/数据设计和子系统设计/子系统设计/状态图/AI智商情商状态图UML.txt)
- [华清池AI伴侣匹配状态图UML.txt](软件详细设计/数据设计和子系统设计/子系统设计/状态图/华清池AI伴侣匹配状态图UML.txt)
- [AIEvaluationParentActivityUML.txt](软件详细设计/数据设计和子系统设计/子系统设计/活动图/AIEvaluationParentActivityUML.txt)
- [AIEvaluationChildLoopActivityUML.txt](软件详细设计/数据设计和子系统设计/子系统设计/活动图/AIEvaluationChildLoopActivityUML.txt)
- [HuaqingMatchingParentActivityUML.txt](软件详细设计/数据设计和子系统设计/子系统设计/活动图/HuaqingMatchingParentActivityUML.txt)
- [HuaqingMatchingChildActivityUML.txt](软件详细设计/数据设计和子系统设计/子系统设计/活动图/HuaqingMatchingChildActivityUML.txt)

## 3. 总体架构

系统采用前后端分离架构：

### 3.1 前端层（Vercel）

前端负责用户交互、页面展示和请求调度，主要页面包括：

- 首页与产品介绍页
- 登录页
- 注册页
- AI 智商/情商评估页
- 评估结果页
- 华清池 AI 伴侣匹配页
- 匹配结果页
- 管理配置页

### 3.2 后端层（Supabase）

后端负责认证、业务数据存储、状态管理和服务编排，主要由以下能力构成：

- Supabase Auth：账号注册、登录、身份验证
- Supabase Database：业务数据存储
- Supabase Edge Functions：评估流程、匹配算法、报告生成等服务逻辑
- Row Level Security：按用户角色控制数据访问范围

### 3.3 业务分层

结合 UML 类图，系统逻辑可分为四层：

- 表现层：LoginUI、RegistrationUI、EvaluationUI、HuaQingUI
- 控制层：LoginManager、RegistrationManager、EvaluationServer、MatchingController
- 服务层：EvaluationManager、HuaQingManager、EvaluatorManager、ProfileBuilder、ReportGenerator
- 数据/算法层：UserDatabase、QuestionRepository、AIModelEngine、ScoringEngine、MatchingEngine、EvaluationLib、HuaQingLib

## 4. 功能模块设计

### 4.1 用户管理模块

对应类：User、Administrator、Evaluator、LoginManager、EvaluatorManager、LoginUI、RegistrationUI、EvaluatorLib。

主要职责：

- 用户注册
- 用户登录
- 身份验证
- 评估者资格校验
- 系统参数配置权限控制

主要流程：

1. 用户提交手机号、密码或注册信息。
2. LoginManager 或 RegistrationManager 做格式校验。
3. 调用 Supabase Auth 或数据库完成身份核验、查重和创建账号。
4. 返回成功或失败结果。

### 4.2 AI 智商/情商评估模块

对应类：EvaluationServer、EvaluationUI、EvaluationManager、AIUnderTest、EvaluationLib、EvaluationQuestionnaire。

主要职责：

- 创建评估任务会话
- 获取评估题目
- 调用 AI 生成回答
- 对回答进行评分
- 保存中间结果
- 汇总整体分数并生成报告

主要流程：

1. 用户发起评估请求。
2. 系统校验用户合法性并创建 Session。
3. 进入循环取题、提交问题、接收 AI 回答、评分、保存中间结果。
4. 满足终止条件后汇总所有结果并生成最终报告。

### 4.3 华清池 AI 伴侣匹配模块

对应类：HuaQingUI、HuaQingManager、HuaQingLib。

主要职责：

- 发起匹配请求
- 收集问卷答案
- 构建用户画像
- 从候选 AI 中筛选推荐对象
- 计算兼容度并排序
- 输出匹配报告

主要流程：

1. 用户提交匹配请求。
2. 系统提供问卷并收集答案。
3. ProfileBuilder 或匹配服务构建用户画像。
4. 从数据库获取候选 AI 信息。
5. MatchingEngine 或 HuaQingLib 计算总分并排序。
6. 输出最终推荐结果。

### 4.4 系统配置模块

对应类：SystemParameter。

主要职责：

- 配置评估阈值
- 配置题库参数
- 配置匹配权重
- 配置报告模板

管理员通过后台维护参数，系统运行时按参数动态调整评分与匹配策略。

## 5. 核心业务流程设计

### 5.1 登录流程

- 用户提交手机号和密码。
- LoginManager 先做格式校验。
- 通过后查询用户库进行身份核验。
- 成功则进入系统，失败返回错误原因。

### 5.2 注册流程

- 用户提交用户名、密码、手机号。
- RegistrationManager 封装注册请求对象。
- 校验格式后检查用户名和手机号是否重复。
- 不重复则写入数据库并返回注册成功。

### 5.3 评估流程

- 用户发起评估请求。
- 系统创建评估会话。
- 在评估循环中持续获取题目、提交给 AI、评分并保存结果。
- 任务结束后汇总分数，生成报告并返回给用户。

### 5.4 匹配流程

- 用户提交匹配请求并完成问卷。
- 系统构建用户画像。
- 从候选 AI 数据集中提取可匹配对象。
- 计算各候选对象的兼容度分数并排序。
- 返回匹配结果报告。

## 6. 状态机设计

### 6.1 AI 智商/情商评估状态

评估任务状态按 UML 可划分为：

- Uninitialized：未初始化
- Initializing：初始化中
- Ready：可开始评估
- Evaluating：评估中
- Finalizing：结果汇总中
- Completed：已完成
- Cancelled：已取消
- Aborted：已中止

其中 Evaluating 由“取题 - 等待 AI 回答 - 评分 - 保存结果”组成循环，直到满足结束条件。

### 6.2 华清池 AI 伴侣匹配状态

匹配任务状态按 UML 可划分为：

- Idle：空闲
- QuestionnairePending：问卷待填写
- Profiling：画像构建中
- CandidateRetrieval：候选检索中
- Matching：匹配计算中
- DeliveringResult：结果发送中
- Completed：已完成
- Cancelled：已取消
- Aborted：已中止
- MatchFailed：匹配失败

## 7. 数据设计

建议在 Supabase 中建立以下核心数据表：

### 7.1 用户表 users

- id
- username
- phone_number
- password_hash 或由 Supabase Auth 托管
- role
- created_at

### 7.2 评估任务表 evaluation_tasks

- id
- user_id
- evaluation_type
- status
- session_id
- overall_score
- created_at
- finished_at

### 7.3 评估题库表 evaluation_questions

- id
- title
- question_type
- question_text
- difficulty
- active

### 7.4 评估答题记录表 evaluation_answers

- id
- task_id
- question_id
- ai_answer
- score
- created_at

### 7.5 匹配任务表 matching_tasks

- id
- user_id
- status
- profile_json
- result_json
- created_at
- finished_at

### 7.6 匹配候选表 ai_candidates

- id
- ai_name
- personality_tags
- interest_tags
- emotion_tags
- capability_score
- active

### 7.7 系统参数表 system_parameters

- key
- value
- updated_by
- updated_at

## 8. 接口设计

建议使用 Supabase Auth + Edge Functions + Database RPC 的方式组织接口。

### 8.1 认证接口

- 注册
- 登录
- 登出
- 获取当前用户信息

### 8.2 评估接口

- 创建评估会话
- 获取下一题
- 提交 AI 回答
- 保存评分结果
- 获取评估报告

### 8.3 匹配接口

- 创建匹配任务
- 提交问卷答案
- 获取候选 AI 列表
- 执行匹配计算
- 获取匹配报告

### 8.4 管理接口

- 维护题库
- 维护候选 AI 数据
- 维护系统参数
- 查看历史任务与报告

## 9. 页面设计

### 9.1 首页

- 展示 Turing Balance 品牌
- 简要介绍评估和匹配能力
- 提供登录、注册、开始评估、开始匹配入口

### 9.2 登录/注册页

- 手机号、用户名、密码输入
- 表单校验
- 失败提示与成功跳转

### 9.3 评估页

- 显示当前题目
- 展示 AI 回答状态
- 显示进度条和计时信息
- 支持中止和结束

### 9.4 评估结果页

- 展示智商分和情商分
- 展示维度分析图
- 展示题目级别明细

### 9.5 匹配页

- 展示问卷
- 支持提交答案
- 展示候选列表和匹配过程

### 9.6 匹配结果页

- 展示推荐排序
- 显示每个 AI 的兼容度、兴趣匹配度、情绪适配度
- 支持导出报告

## 10. 部署方案

### 10.1 前端部署

- 使用 Vercel 托管 Next.js 前端
- 利用环境变量配置 Supabase URL 和匿名密钥
- 构建后自动部署到 Vercel

### 10.2 后端部署

- 使用 Supabase 作为 BaaS 平台
- 数据库承担核心持久化
- Edge Functions 承担评估和匹配算法调用
- 使用 RLS 控制数据访问权限

## 11. 安全与权限

- 登录注册由 Supabase Auth 统一管理
- 普通用户仅能访问自己的评估和匹配记录
- 管理员可维护系统参数和题库
- 对敏感结果和用户数据启用行级权限控制

## 12. 结论

Turing Balance 的软件结构已经可以清晰划分为“用户管理 + AI 评估 + 伴侣匹配 + 系统配置”四个核心模块。UML 中的状态流转和活动图已经足以支撑落地为一个完整的前后端分离应用，且与 Vercel + Supabase 的部署方式天然匹配。
